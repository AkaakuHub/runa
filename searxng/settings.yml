use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "runa-searxng-local-only-change-before-public-use"
  limiter: false
  image_proxy: false

search:
  formats:
    - html
    - json
